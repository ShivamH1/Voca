{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": ".venv/Scripts/python -m uvicorn main:app --port 8000 --reload --reload-dir . --reload-exclude .venv --reload-exclude qdrant_db --reload-exclude downloads",
    "build": "echo 'No build step required for FastAPI'"
  }
}
