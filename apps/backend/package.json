{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nodemon --watch . --ext py --exec .venv/Scripts/python -m uvicorn main:app --port 8000",
    "build": "echo 'No build step required for FastAPI'"
  }
}
